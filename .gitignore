secrets.h

# macOS metadata
.DS_Store

# Claude Code local config (user-specific)
.claude/settings.local.json

# Logs (kept locally, not committed)
logs/*.log
logs/serial/
# One per flash.sh run, plus the flash-latest.log symlink
tests/visual/runs/*.log

# Arduino build artifacts
Test_Button_DMX/build/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
