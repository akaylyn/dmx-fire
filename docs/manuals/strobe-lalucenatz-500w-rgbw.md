# LaluceNatz 960 RGBW Strobe/Wash Panel 8×8

**Item number:** LL960S  
**Source manual:** https://efxproductions.com/wp-content/uploads/2025/07/LED-960-RGBW-Strobe-Manual.pdf  
**Product page:** https://www.lalucenatzonline.com/products/500w-960led-rgb-w-dj-strobe-light-panel-with-zone-control-for-concert-disco-party-light-free-shipping

---

## Specifications

| Parameter | Value |
|-----------|-------|
| Power consumption | 240W |
| Voltage | AC 100–240V, 50/60Hz |
| Power connector | PowerCon in / out |
| LEDs | 864 × SMD5050 RGB 3-in-1 + 96 × SMD5050 White |
| Total LEDs | 960 |
| Segments | 8 (arranged in 8×8 grid) |
| Features | 8-segment control + wash + flashing |
| DMX channels | 4 / 11 / 32 / 39 CH (selectable) |
| DMX connector | 3-pin XLR in / out |
| Control modes | DMX512 / Master–Slave / Auto / Sound |
| Strobe | 1–30 Hz or random |
| Dimming | 0–100% linear |
| IP rating | IP20 |
| Operating temp | 14°F – 113°F (−10°C – 45°C) |
| Lifespan | ~50,000 hours |
| Weight | 8.38 lbs (3.8 kg) |
| Dimensions | 18 × 3.5 × 10 in (W × D × H) |

---

## Front panel controls

| Button | Function |
|--------|----------|
| MENU | Enter menu / confirm |
| UP | Increase parameter |
| DOWN | Decrease parameter |
| ENTER | Confirm and save (with power-off memory) |

---

## Menu function table

| Display | Range | Description |
|---------|-------|-------------|
| `A001`–`A512` | 1–512 | DMX start address (default: A001) |
| `CH04` / `CH11` / `CH32` / `CH39` | — | Channel mode select (default: CH11) |
| `M000`–`M083` | 0–83 | RGB built-in effect (84 programs); every 3 values = one effect |
| `S000`–`S255` | 0–255 | RGB effect speed |
| `M000`–`M040` | 0–40 | White built-in effect (41 programs); every 6 values = one effect |
| `S000`–`S255` | 0–255 | White effect speed |
| `Soud` | — | Sound control mode |
| `R000`–`R255` | 0–255 | Red channel level (default: R255) |
| `G000`–`G255` | 0–255 | Green channel level (default: G255) |
| `B000`–`B255` | 0–255 | Blue channel level (default: B255) |
| `W000`–`W255` | 0–255 | White channel level (default: W255) |
| `T000`–`T###` | — | Temperature display in °C (read-only) |

---

## DMX channel tables

### 4-channel mode

| CH | Range | Function |
|----|-------|----------|
| 1 | 0–255 | Red linear dimming |
| 2 | 0–255 | Green linear dimming |
| 3 | 0–255 | Blue linear dimming |
| 4 | 0–255 | White linear dimming |

---

### 11-channel mode

| CH | Range | Function |
|----|-------|----------|
| 1 | 0–255 | Master dimmer |
| 2 | 0–255 | RGB strobe speed (0 = off) |
| 3 | 0–255 | RGB mode (84 effects; 0–2 = off, 3–255 every 3 values = one effect) |
| 4 | 0–255 | RGB mode speed |
| 5 | 0–255 | Red linear dimming |
| 6 | 0–255 | Green linear dimming |
| 7 | 0–255 | Blue linear dimming |
| 8 | 0–255 | White strobe speed (0 = off) |
| 9 | 0–255 | White mode (41 effects; 0–5 = off, 6–255 every 6 values = one effect) |
| 10 | 0–255 | White mode speed |
| 11 | 0–255 | White linear dimming |

---

### 32-channel mode

Individual RGB control per segment + white per segment. 8 segments total.

| CH | Range | Function |
|----|-------|----------|
| 1 | 0–255 | R segment 1 |
| 2 | 0–255 | G segment 1 |
| 3 | 0–255 | B segment 1 |
| 4 | 0–255 | R segment 2 |
| 5 | 0–255 | G segment 2 |
| 6 | 0–255 | B segment 2 |
| 7 | 0–255 | R segment 3 |
| 8 | 0–255 | G segment 3 |
| 9 | 0–255 | B segment 3 |
| 10 | 0–255 | R segment 4 |
| 11 | 0–255 | G segment 4 |
| 12 | 0–255 | B segment 4 |
| 13 | 0–255 | R segment 5 |
| 14 | 0–255 | G segment 5 |
| 15 | 0–255 | B segment 5 |
| 16 | 0–255 | R segment 6 |
| 17 | 0–255 | G segment 6 |
| 18 | 0–255 | B segment 6 |
| 19 | 0–255 | R segment 7 |
| 20 | 0–255 | G segment 7 |
| 21 | 0–255 | B segment 7 |
| 22 | 0–255 | R segment 8 |
| 23 | 0–255 | G segment 8 |
| 24 | 0–255 | B segment 8 |
| 25 | 0–255 | W segment 1 |
| 26 | 0–255 | W segment 2 |
| 27 | 0–255 | W segment 3 |
| 28 | 0–255 | W segment 4 |
| 29 | 0–255 | W segment 5 |
| 30 | 0–255 | W segment 6 |
| 31 | 0–255 | W segment 7 |
| 32 | 0–255 | W segment 8 |

---

### 39-channel mode

Global effects + per-segment RGB and white control.

| CH | Range | Function |
|----|-------|----------|
| 1 | 0–255 | Master dimmer |
| 2 | 0–255 | RGB strobe speed (0 = off) |
| 3 | 0–255 | RGB mode (84 effects; 0–2 = off, every 3 values = one effect) |
| 4 | 0–255 | RGB mode speed |
| 5 | 0–255 | R segment 1 |
| 6 | 0–255 | G segment 1 |
| 7 | 0–255 | B segment 1 |
| 8 | 0–255 | R segment 2 |
| 9 | 0–255 | G segment 2 |
| 10 | 0–255 | B segment 2 |
| 11 | 0–255 | R segment 3 |
| 12 | 0–255 | G segment 3 |
| 13 | 0–255 | B segment 3 |
| 14 | 0–255 | R segment 4 |
| 15 | 0–255 | G segment 4 |
| 16 | 0–255 | B segment 4 |
| 17 | 0–255 | R segment 5 |
| 18 | 0–255 | G segment 5 |
| 19 | 0–255 | B segment 5 |
| 20 | 0–255 | R segment 6 |
| 21 | 0–255 | G segment 6 |
| 22 | 0–255 | B segment 6 |
| 23 | 0–255 | R segment 7 |
| 24 | 0–255 | G segment 7 |
| 25 | 0–255 | B segment 7 |
| 26 | 0–255 | R segment 8 |
| 27 | 0–255 | G segment 8 |
| 28 | 0–255 | B segment 8 |
| 29 | 0–255 | White strobe speed (0 = off) |
| 30 | 0–255 | White mode (41 effects; 0–5 = off, every 6 values = one effect) |
| 31 | 0–255 | White mode speed |
| 32 | 0–255 | W segment 1 |
| 33 | 0–255 | W segment 2 |
| 34 | 0–255 | W segment 3 |
| 35 | 0–255 | W segment 4 |
| 36 | 0–255 | W segment 5 |
| 37 | 0–255 | W segment 6 |
| 38 | 0–255 | W segment 7 |
| 39 | 0–255 | W segment 8 |

---

## Built-in effects summary

| Type | Count | DMX encoding |
|------|-------|-------------|
| RGB effects | 84 | CH value 3–255; every 3 values = one effect |
| White effects | 41 | CH value 6–255; every 6 values = one effect |

RGB effect types include: single colours (R/G/B/RG/RB/GB/RGB), full colour, 1/2-segment, racing, flowing, refresh, stacking.

White effect types include: white, 1/2/3/4-segment, racing, flowing, refresh, stacking.

---

## Troubleshooting: fixture ignores DMX / runs its own pattern

Observed on site: all four uplights showing a yellow field with a white runner and
refusing to respond to either the ESP32 or a manual DMX console.

**There is no separate "DMX mode" menu item on this fixture.** Selecting a built-in
effect (`M###`) or `Soud` *is* the mode change, and `ENTER` saves it with power-off
memory. A fixture with an effect selected ignores DMX from any controller until the
effect is cleared. The observed pattern is an RGB *racing/flowing* effect plus a
*white* effect running together — note the RGB and white effect banks are separate
menu items that both display as `M###`.

**The fixture also runs a built-in demo program when it receives no valid DMX at
all — and on site this turned out to be the actual cause.** A fixture showing a
pattern is therefore not evidence of a menu misconfiguration; check signal first.

Quick triage — **unplug the fixture's DMX cable and watch it**:

| Result | Cause | Fix |
|--------|-------|-----|
| Pattern continues, and reconnecting restores DMX control | **No valid DMX was reaching it.** Suspect a dead link *upstream* — a failed pass-through, bad cable, or a failed fixture input stage clamping the pair | Find and replace the bad node/cable; see below |
| Pattern continues even with good signal present | Standalone effect mode latched in the menu | Clear both `M###` effects, then set `CH04` |
| Goes dark / static | Fixture is in `CH11`, so incoming Blue lands on its effect-select channel (CH3) | Set `CH04` |

The `CH11` case also makes a plain 4-fader RGBW console look broken: its blue fader
is CH3, the effect selector.

**One bad fixture can take out the whole universe.** A failed RS-485 input stage
clamps the differential pair and corrupts frames for *every* device on the bus,
including upstream ones and including a manual console; a failed XLR pass-through
silently kills everything downstream of it. Symptom shape: fixtures and decoders
upstream of the fault behave normally, everything downstream reverts to demo
patterns or goes dead. Bisect the chain by unplugging fixtures one at a time and
watching what recovers.

**Reset to plain 4-channel DMX control, per fixture:**

1. `CH##` → **`CH04`** → ENTER *(default is `CH11`; independent of the address)*
2. RGB effect `M###` (ranges to `M083`) → **`M000`** → ENTER
3. White effect `M###` (ranges to `M040`) → **`M000`** → ENTER — easy to miss, displays the same as the RGB one
4. Step past `Soud` **without** pressing ENTER
5. `A###` → tower address (**009 / 024 / 039 / 054**) → ENTER
6. Confirm manual `R`/`G`/`B`/`W` levels are `255`, and that the factory output limits (hold MENU 3 s) are near the default `200`, not `032`

**Verify:** set the project's theme to `rainbow` — each tower gets a hue offset by
index, so all four uplights should show four *different* colours. Same colour on
multiple fixtures means they share a DMX address. Then `bright_white` at 255 should
drive all four to full white, exercising the W channel.

> Avoid the `Send` item in the factory-settings menu (hold MENU 3 s) — it broadcasts
> settings to every daisy-chained fixture, which is one way all fixtures end up
> configured identically.

---

## Master / Slave operation

- Connect fixtures via 3-pin XLR daisy-chain
- Set one fixture as primary (any address A001–A512)
- All secondaries sync to primary automatically
- Only one primary per group — multiple primaries cause random unsynchronised output

---

## Factory settings (hold MENU 3 seconds)

| Display | Range | Default | Function |
|---------|-------|---------|----------|
| R032–R255 | 32–255 | 200 | Red output limit |
| G032–G255 | 32–255 | 200 | Green output limit |
| B032–B255 | 32–255 | 200 | Blue output limit |
| W032–W255 | 32–255 | 200 | White output limit |
| FAN0 / FAN1 | — | FAN0 | FAN0 = on with LEDs; FAN1 = on at temp threshold |
| T040–T105 | 40–105°C | 60°C | Temperature protection point |
| Send | — | — | Broadcast factory settings to all daisy-chained fixtures |

---

## Wiring

```
Controller XLR out ──► Strobe XLR in ──► Next fixture (XLR Thru)
                                              └── 120Ω terminator on last fixture
```

Safety: secure fixture to truss with both a clamp and a safety rope through the base holes. Keep at least 1.5 ft from combustible materials.
